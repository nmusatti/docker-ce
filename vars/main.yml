---
dockerce_platform: "{{ ansible_distribution }}{{ 
( ansible_distribution == 'CentOS' ) | ternary(ansible_distribution_major_version, '') }}"
dockerce_distribution: "{{ ansible_distribution | lower }}"
